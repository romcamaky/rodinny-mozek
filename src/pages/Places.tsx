function Places() {
  return (
    <section className="mx-auto w-full max-w-md">
      <h1 className="text-3xl font-bold">Místa</h1>
      <p className="mt-3 text-base" style={{ color: 'var(--color-text-secondary)' }}>
        Databáze míst — připravujeme.
      </p>
    </section>
  )
}

export default Places
